version: "3.9"
services:
  app:
    build: .
    ports:
      - "8080:8000"
    depends_on:
      - db
    environment:
      - DATABASE_HOST=db

  db:
    image: postgres:16
    environment:
      - POSTGRES_PASSWORD=mypass
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
